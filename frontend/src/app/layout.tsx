import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
});

export const metadata: Metadata = {
  title: "GrantAgent — AI-Native Funding Operating System",
  description: "AI-powered autonomous grant application platform. Discover grants, evaluate eligibility, generate proposals, validate compliance, and prepare submission-ready applications.",
  keywords: ["grants", "AI", "funding", "startup", "nonprofit", "SBIR", "proposal writing"],
};

import { AuthProvider } from "@/components/AuthProvider";

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="dark">
      <body className={`${inter.variable} font-sans antialiased`}>
        <AuthProvider>
          {children}
        </AuthProvider>
      </body>
    </html>
  );
}
